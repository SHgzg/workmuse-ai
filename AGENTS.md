# WorkMuse 项目协作说明

## 产品目标

WorkMuse 是以会议材料、知识检索、目标、任务和成果为核心的 AI 工作助手。

核心闭环：材料收集 → 搜索溯源 → 目标与任务 → 成果记录 → 差距分析 → 优化执行。

## 权威文档

- 总体需求：`docs/product/product-requirements.md`
- UI 规划：`docs/product/ui-plan.md`
- 模块规则：`docs/modules/README.md`
- 技术架构：`docs/architecture/`
- 重要决策：`docs/decisions/`

实现功能前先阅读对应文档。代码、设计和文档冲突时，先指出冲突，不自行改写产品规则。

## 核心产品规则

- AI 的关键结论必须可追溯到原始材料。
- 明确区分原文、系统摘要、AI 推断和待确认信息。
- 权限同时作用于搜索、预览、导出和 AI 问答。
- 任务应能追溯到目标、会议、观点、灵感或优化建议。
- “完成任务”和“取得成果”是不同概念。
- 差距分析必须展示计算依据、数据来源和不确定性。
- 默认优先本地处理；云端处理必须遵守用户授权和数据范围。

## 开发约定

- 修改前检查现有实现、相关需求和架构文档。
- 优先复用现有组件、类型、协议和数据模型。
- 未经确认不引入新的生产依赖。
- 数据结构变化必须考虑迁移、回滚和旧数据兼容。
- 新功能包含加载、空状态、失败、无权限和离线状态。
- 不把密钥、令牌或真实用户数据提交到仓库。
- Renderer 不直接执行系统命令；高权限能力由主进程或受控 Worker 提供。

## 验证要求

- TypeScript 变更至少运行 `pnpm typecheck`。
- 构建、配置或跨进程协议变更运行 `pnpm build`。
- Python Worker 变更运行对应测试或最小协议验证。
- 修改检索、权限、引用或差距计算时，补充关键成功与失败场景验证。

## 文档同步

以下变化必须同步更新文档：

- 用户流程、功能范围或验收标准变化；
- 数据模型、权限或跨进程协议变化；
- AI 检索、引用、推断或隐私行为变化；
- 做出重要且难以撤销的技术决策。

`AGENTS.md` 只保存长期稳定的协作约束。详细业务需求放在 `docs/`，只有子目录存在不同的开发规则时才增加局部 `AGENTS.md`。
